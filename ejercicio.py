cantidad_acciones_vendidas=int(input("¿Cuántas acciones A se vendieron en "+ str(i) + " hora"))

cantidad_total_acciones_vendidas=0

for i in range(1,3):

    cantidad_acciones_vendidas=int(input("¿Cuántas acciones A se vendieron en " + str(i) + " hora"))

 

print(cantidad_total_acciones_vendidas)

